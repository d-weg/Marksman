/target
**/*.rs.bk
.codeindex/
.codeindex-rs/
.peashooter/
.DS_Store

# agent-bench: never commit the API key
scripts/agent-bench/.key

# local dogfood: machine-specific MCP config (absolute paths)
.mcp.json

# per-machine agent settings
.claude/
